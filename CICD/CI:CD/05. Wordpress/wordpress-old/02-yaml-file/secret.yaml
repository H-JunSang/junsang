cat <<EOF > ./kustomization.yaml
secretGenerator:
- name: mysql-pass
  literals:
  - password=qwer1234!@#$
resources:
  - mariadb-deployment.yaml
  - wordpress-deployment.yaml
EOF

# kubectl apply -k ./
# 리소스 파일 경로에서 해당 명령어로 배포